# Allow all crawlers
User-agent: *
Allow: /

# Prevent crawling of error pages
Disallow: /404
Disallow: /500

# Prevent crawling of potential sensitive paths
# Disallow: /private/

Sitemap: https://vancura.dev/sitemap-index.xml

# Crawl-delay for heavy crawlers (optional)
# Crawl-delay: 10
