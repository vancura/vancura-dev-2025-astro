---
import strings from '../../strings.mjs';
import Content from '../content/pages/404.mdx';
import Layout from '../layouts/Layout.astro';
---

<Layout
    title={`404 • ${strings.title}`}
    description="Page not found."
    ogImage="https://whitepaper.actor/opengraph.png"
    permalink={Astro.url.toString()}
>
    <header class="mdx-content mx-auto max-w-(--breakpoint-lg)" role="banner">
        <Content />
    </header>
</Layout>
